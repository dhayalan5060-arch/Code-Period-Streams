import java.util.*;
import java.util.stream.Collectors;
import java.util.function.Function;
class Main {
    public static void main(String[] args) {
        
        String[]  s = {"abc", "123", "xyz" };
        
        List<Integer> str = Arrays.stream(s)
                                 .filter(x -> x.matches("\\d+"))    \\ ("\\d+") It matches one or more digits.
                                 .map(Integer::parseInt)           \\ converting string into integer
                                 .collect(Collectors.toList());
        
       
        System.out.println(str);
    }
}

//Without Using .map(Integer :: parseInt)
List<String> num = Arrays.stream(s)
                       .filter(x ->x.matches("\\d+"))
                       .collect(Collectors.toList());

