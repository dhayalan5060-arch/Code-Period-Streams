import java.util.*;
class reverse {
    public static void main(String[] args) {                          //String does not support stream method, we need to convert it to an array and use stream
        String s = " I am learning Stream Api in Java";               // 1)Using split() + Arrays.stream()                    
        String ans = Arrays.stream(s.split("\\s+"))                    //2) Use pattern.SplitStream 
                            .max(Comparator.comparing(String::length)) //   Pattern.compile("\\s+")
                            .get();                                    //.splitAsStream(s)
        System.out.println(ans);                                       //.forEach(System.out::println)
        
    
    }                                                               //3) Convert String to Character Stream
}                                                                   //s.chars()
                                                                    //.mapToObj(c -> (char) c)
                                                                    // .forEach(System.out::println);

//Arrays.stream(s.split("\\s+"))
  //    .sorted(Comparator.comparing(String::length))
    //  .forEach(System.out::println);

//print in ascending order based on word length

//List<String> words = Arrays.stream(s.split("\\s+"))
 //                          .sorted(Comparator.comparing(String::length))         // we can use list, it stores string [I, am, in, Api, Java, Stream, learning]
 //                          .toList();

//System.out.println(words);
