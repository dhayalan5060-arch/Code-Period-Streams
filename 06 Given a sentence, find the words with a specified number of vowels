import java.util.*;
import java.util.stream.Collectors;
import java.util.function.Function;

class Main {
    public static void main(String[] args) {
        
        String s = "I am learning Streams API in Java";
        
              Arrays.stream(s.split("\\s+"))                             //("[^aeiouAEIOU]","") --> replace all which charachter dont have ""
              .filter(x ->x.replaceAll("[^aeiouAEIOU]","").length()==2)
              .forEach(System.out::println);
      
    }
}

import java.util.*;
import java.util.stream.Collectors;
import java.util.function.Function;
class Main {
    public static void main(String[] args) {
    
  String s = "Dheena is a backend developer at Robert Bosch Bosch";
    
List<String> vowels = Arrays.stream(s.split("\\s+"))
                            .map(x -> x.replaceAll("[^aeiouAEIOU]",""))
                             .filter(x ->!x.isEmpty())                          // remove spaces from list
                            .collect(Collectors.toList());
                        
   
               
        System.out.println(vowels);
    }
}
